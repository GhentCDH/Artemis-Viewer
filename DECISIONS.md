# Component decisions

- `LocationPing` is a shared base component because projecting and transiently marking a geographic coordinate is independent of the search feature and can be reused by any map-driven interaction.
